// project import
import prospectoVendedor from './prospectoVendedor';
import vendedor from './vendedor';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [prospectoVendedor, vendedor]
};

export default menuItems;
